# Copiar a .env y completar la contraseña (no versionar .env)
# cp .env.template .env
#
# Cadena de referencia (Neon):
# postgresql://DB_USER:DB_PASS@DB_HOST/DB_NAME?sslmode=require

# Neon PostgreSQL
DB_PROVIDER=postgres
DB_USER=neondb_owner
DB_PASS=
DB_HOST=ep-fancy-frog-acjvuvma-pooler.sa-east-1.aws.neon.tech
DB_NAME=neondb

API_HOST=0.0.0.0
API_PORT=8000

FRONTEND_URL=http://localhost:3000

# Discord OAuth (app en Discord Developer Portal)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=http://localhost:8000/auth/callback

# Verificación de miembro y roles (bot en el servidor Evoluciona)
DISCORD_GUILD_ID=
DISCORD_BOT_TOKEN=

# IDs de rol en Discord (Developer Mode → copiar ID del rol)
ROLE_ID_EVOLUCIONA=
ROLE_ID_EQUIPO=
ROLE_ID_CONSULTORIA=
# Pre-consultoría: sin ROLE_ID — no se mapea; esos usuarios no entran

SESSION_SECRET=
